Googletranslate
===========

Installation
------------

.. code:: bash

    $ pip install googletranslate

Update after forked
--------------------

Add `~/.googletrans` for configuration file to custom google server host.
